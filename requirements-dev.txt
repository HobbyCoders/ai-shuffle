# Development dependencies for AI Shuffle
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
httpx>=0.27.0  # Already in requirements.txt, but needed for TestClient

# Time mocking for auth tests
freezegun>=1.2.0

# Linting (already using ruff via CLI)
ruff>=0.1.6

# Type checking (optional)
# mypy>=1.7.0
